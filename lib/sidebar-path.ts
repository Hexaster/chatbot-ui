import { ContentType } from "@/types"

export const SIDEBAR_PATH: Record<ContentType, string> = {
  chats: "/chats",
  presets: "/presets",
  prompts: "/prompts",
  models: "/models",
  files: "/files",
  collections: "/collections",
  assistants: "/assistants",
  tools: "/tools"
}
